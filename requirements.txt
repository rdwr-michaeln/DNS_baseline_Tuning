# DefensePro Failover Manager — Python dependencies
#
# Install into a virtual environment:
#   python3 -m venv myvenv
#   source myvenv/bin/activate
#   pip install -r requirements.txt

# Cyber Controller REST API client
requests>=2.32.5
urllib3>=2.5.0

# Cryptography backend (required by paramiko for SSH)
cryptography>=43.0

# SSH client — used for pushing config templates to DefensePro devices via CLI
paramiko>=3.0
