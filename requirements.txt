# DefensePro Failover Manager — Python dependencies
#
# Install into a virtual environment:
#   python3 -m venv myvenv
#   source myvenv/bin/activate
#   pip install -r requirements.txt

# Cyber Controller REST API client
requests>=2.32.5
urllib3>=2.5.0

# SNMP trap receiver (v2c and v3 authPriv)
pysnmp>=7.1.21
pyasn1>=0.6.1

# Cryptography backend required by pysnmp for SNMPv3 auth/privacy
# (DES, AES-128, AES-256 encryption; MD5, SHA auth)
# Requires >= 43.0 — earlier versions do not expose the 'decrepit' DES module
cryptography>=43.0
